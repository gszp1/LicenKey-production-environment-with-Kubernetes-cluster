apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: backend-ns
data:
  datasource_url: postgres-service.postgres-db-ns:5432
  kafka_bootstrap_server: kafka-cluster-kafka-bootstrap.kafka-cluster-ns:9092
  retries: '3'
  acks: all
  application_port: '9000'
